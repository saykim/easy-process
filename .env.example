# Vercel Postgres Database Configuration
# After creating a Vercel Postgres database, these values will be automatically populated
# when you deploy to Vercel. For local development, add these to .env.local

# Postgres Database URL
POSTGRES_URL="postgres://username:password@host:port/database"

# Postgres Database URL (no SSL - for some local development tools)
POSTGRES_URL_NON_POOLING="postgres://username:password@host:port/database?sslmode=require"

# Individual connection details (auto-populated by Vercel)
POSTGRES_USER="username"
POSTGRES_HOST="host"
POSTGRES_PASSWORD="password"
POSTGRES_DATABASE="database"

# Note: When deploying to Vercel with a Postgres database attached,
# these environment variables are automatically set - no manual configuration needed!
